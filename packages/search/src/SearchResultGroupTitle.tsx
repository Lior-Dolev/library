import { FC } from 'react';

interface ISearchResultGroupTitleProps {
  primaryText: string;
  captionText?: string;
  count?: number;
}

const SearchResultGroupTitle: FC<ISearchResultGroupTitleProps> = ({}) => {
  return <div>SearchResultGroupTitle</div>;
};

export default SearchResultGroupTitle;
